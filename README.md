# Dialiqo Website (static)

Pure static frontend: **HTML + Tailwind CSS + custom CSS + vanilla JS**.  
**No Next.js / React** at runtime or in dependencies.

- Live site files: [`site/`](./site/)
- Preview: `npm run serve` → http://localhost:4173
- Previous Next.js app (reference only): [`_archive/next-src/`](./_archive/next-src/)
